package constants

// DefaultRootUserRootKey is the key used in the database (e.g., in the 'meta' or 'admin' column family)
// to store information or metadata related to the default root user.
// This could be used to store the initial root user's details or a flag indicating its creation status.
const DefaultRootUserRootKey = "default_root_user"
